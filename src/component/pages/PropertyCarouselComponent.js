import { Carousel } from "react-bootstrap";

import { LinkContainer } from "react-router-bootstrap";

const PropertyCarouselComponent = () => {
   
    return (
        // Carousel for Best Latest properties (Houses)
        <div>
            Carousel
        </div>
    );
}

export default PropertyCarouselComponent;